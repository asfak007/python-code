# lambda is a Ananymous or inline function. its like a one line funvtion and its soooo good. i like that
# lambda is use for if we need  a one line function.
# lambda has limitaion we just wright a one statement 

add_number = lambda x,y:x+y # add_numbers refare the Ananymous function and return a value (x+y)
print(add_number(2,9))
print()

Programmer = lambda name : name + "is a good programmer."

print(Programmer("Robin"))